Test2:
Add Reward: +1 when hit ball.
	    -1 when lose.

Input:
Agent: 	position y.
	velocity y.

Ball:	position x.
	position y.
	velocity x.
	velocity y.

Notes:
random velocity tweak when collide (first quarter).
random ball reset (position, velocity).
auto reset when ball is away too long (10s, first agent).